// Assign-306

public class TimeConversion {
    public static void cal(Integer hours) {
        Long seconds = hours * 60 * 60;
        System.debug('Hours: ' + hours);
        System.debug('Hours: ' + seconds);
    }
}